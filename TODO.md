# TODO

## Export / Import data (Option 1)
Add export and import buttons to serialize all localStorage keys to a `.json` file (export) and restore from one (import). No dependencies, no backend. Good for backups and one-time device migrations.

See conversation: "elegant way to make localStorage exportable / sync between devices"
